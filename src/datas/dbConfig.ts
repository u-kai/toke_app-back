export type DBConfig = {
    user: string
    password: string
    host: string
    database: string
}
export const dbConfig: DBConfig = {
    user: 'root',
    password: 'oyyg83019%',
    host: 'localhost',
    database: 'toke_app',
}
